{
  "name": "js-vault",
  "version": "0.0.4",
  "description": "Javascript storage vaults",
  "main": "dist/index.js",
  "repository": "https://github.com/knipferrc/js-vault.git",
  "author": "knipferrc <knipferrc@gmail.com>",
  "license": "MIT",
  "scripts": {
    "prepublishOnly": "rimraf dist && babel src -d dist",
    "build": "rimraf dist && babel src -d dist"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.6.0",
    "babel-preset-stage-0": "^6.24.1",
    "rimraf": "^2.6.1"
  }
}
